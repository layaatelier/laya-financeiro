// Preencha para usar o Supabase (login + banco na nuvem + tempo real).
// Deixe vazio para rodar em modo demonstração (dados só no navegador).
// A chave "anon" é pública por design; a segurança vem das políticas RLS do schema.sql.
export const CONFIG = {
  SUPABASE_URL: '',
  SUPABASE_ANON_KEY: '',
  EMPRESA: 'Laya',
};
